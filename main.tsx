import React from 'react'
import ReactDOM from 'react-dom/client'
import VacinHome from './vacin-home'
import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
    <React.StrictMode>
        <VacinHome />
    </React.StrictMode>,
)
